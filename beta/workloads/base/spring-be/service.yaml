apiVersion: v1
kind: Service
metadata:
  labels:
    organisation: riag
    product-cluster: tech
    product: beta
    application: beta
    container: spring-be
  name: spring-be
  namespace: riag-tech-beta-beta
spec:
  ports:
    - name: http-svc
      port: 80
      protocol: TCP
      targetPort: http
  selector:
    container: spring-be
  type: ClusterIP
